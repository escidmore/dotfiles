# Evelyn's dotfiles
Much of this was shamelessly borrowed from https://github.com/kkragenbrink/dotfiles

## Installation
I'll steal Kevin's bootstrapper later

In order to make use of the bash-git-prompt stuff simply do:
```
cd; git clone https://github.com/magicmonty/bash-git-prompt.git .bash-git-prompt --depth=1
```
